# 5.15 开发计划

## 当前进度概览
Phase 3 前端评测页面重写完成 — 对齐 5 表设计，EvaluationPage + CreateTaskDialog + EvaluationDetailPage。

## 已完成事项
- [x] 项目初始化: .gitignore, 基础配置
- [x] 更新 api/client.ts 响应拦截器 — 自动解包 ApiResponse<T> { code, message, data }
- [x] 更新 types/agent.ts — 对齐后端实体（移除 metrics, 添加 endpointUrl/authType/authCredential）
- [x] 更新 api/modules/agent.ts — 添加 testConnection
- [x] 更新 stores/modules/agent.ts — 添加 create/update/delete/testConnection
- [x] AgentListPage — 真实 API, 搜索, 分页, 新建/编辑弹窗, 删除确认
- [x] AgentDetailPage — 真实 API, 测试连接
- [x] 创建 ConfirmDialog 组件
- [x] 添加 .input-field CSS 样式类
- [x] 修复: app store 缺少 sidebarCollapsed/toggleSidebar
- [x] 修复: SidebarNav.vue 类型错误
- [x] tsconfig 修复: ignoreDeprecations 版本
- [x] TypeScript 类型检查通过
- [x] Agent 精简: 移除 tags 字段（types / 列表页表单 / 详情页展示）
- [x] 鉴权方式新增 custom 选项: 下拉 + computed placeholder（JSON 示例提示）
- [x] 题库类型: src/types/question.ts（Question / Turn / ImportResult / 查询参数）
- [x] 题库 API: src/api/modules/question.ts（8 个接口: CRUD / batch / import / export）
- [x] 题库 Store: src/stores/modules/question.ts
- [x] QuestionListPage: 搜索 + 分类/难度/类型筛选 + 批量删除 + 导入导出
- [x] 多轮编辑: turns 动态增删 + turnOrder 自动编号
- [x] 路由 + 侧边栏: /questions + Library 图标
- [x] 评测类型重写: types/evaluation.ts 对齐 5 表设计（DimensionConfig / AgentResult / TaskProgress）
- [x] 评测 API 重写: api/modules/evaluation.ts 9 个接口（/api/evaluation）
- [x] 评测 Store 重写: stores/modules/evaluation.ts（含 progress 轮询 + results）
- [x] EvaluationPage 重写: 对接 store + 状态筛选 + 启动/取消/删除操作
- [x] CreateTaskDialog: 4 步向导（基本信息 → 选题 → 选Agent → 维度权重配置）
- [x] EvaluationDetailPage 重写: 进度条 + 3s 轮询 + 结果排名 + 维度得分 + 逐题明细

## API / 接口清单
| 方法 | 路径 | 用途 | 状态 |
|------|------|------|------|
| GET | /api/agents | 分页查询 Agent | ✅ |
| GET | /api/agents/:id | Agent 详情 | ✅ |
| POST | /api/agents | 创建 Agent | ✅ |
| PUT | /api/agents/:id | 更新 Agent | ✅ |
| DELETE | /api/agents/:id | 删除 Agent | ✅ |
| POST | /api/agents/:id/test-connection | 测试连接 | ✅ |

## 下一步待做功能
- [ ] Phase 3.2: 后端评测任务接口（9 个 API）
- [ ] Phase 3.3: 评测引擎（AgentHttpClient + 评分）

## 技术决策记录
- API 客户端响应拦截器自动解包 ApiResponse<T>：判断 body.code === 200 且 'data' in body，则将 body.data 返回给调用方
