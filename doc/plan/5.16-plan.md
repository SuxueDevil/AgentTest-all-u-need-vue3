# 5.16 开发计划

## 当前进度概览
前端 UI 优化 — Agent 图标改为每个 Agent 独特色调的淡彩渐变，题库管理图标区分，修复图标缩放问题。

## 已完成事项
- [x] 题库管理侧边栏图标改为 `BookOpen`（与 Agent 管理的 `Bot` 区分）
- [x] Agent 列表首字母图标重构 — 每个 Agent 根据名称哈希生成唯一色相（0~359），淡彩半透明渐变背景，藏青字体
- [x] 修复 Agent 图标在界面缩小时被压缩的 bug（加 `flex-shrink: 0`）
- [x] CLAUDE.md 注释规范更新 — Java 方法必须含 `@param`/`@return`/`@throws`，TS 函数必须含 `@param`/`@returns`
- [x] fix: 评测任务创建"下一步"按钮 disabled 条件按步骤区分（不阻塞前序步骤）
- [x] fix: Agent 详情页增加错误处理（try/catch + 加载失败提示）+ 返回改用 router.push
- [x] fix: App.vue 移除 transition mode="out-in" 过渡动画导致详情页渲染空白
- [x] style: 评测弹窗按钮统一 btn-secondary 风格

## 下一步待做功能
- [ ] Phase 3.2: 评测任务后端接口
- [ ] 后端日志与监控系统方案讨论

## 阻塞 / 风险
- Judge LLM 选型待定（评分引擎需要）
- 多轮对话消息格式待定
