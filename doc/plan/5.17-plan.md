# 5.17 开发计划

## 当前进度概览
Phase 5 优化基本完成。补齐 LLM 管理 + 评测引擎支持 LLM + 进度条/交互优化 + 逻辑删除全覆盖。

## 已完成事项

### 新增 LLM 模型管理
- [x] 后端: LLM Entity + Mapper + Service + Controller + 建表
- [x] 后端: AgentHttpClient.sendToLLM()
- [x] 前端: LLM types + API + Store + 列表页 + 路由 + 导航图标

### 评测支持 LLM
- [x] 前端: 评测创建三列布局（选题 + Agent + LLM）
- [x] 后端: evaluation_task 加 llm_ids，evaluation_result 加 llm_id
- [x] 后端: executeAsync LLM 并行循环 + 结果聚合

### 进度条 + 交互优化
- [x] running 状态流光动画 + 百分比数字
- [x] 自动轮询静默模式，不闪烁
- [x] 点击启动乐观更新 status=running，进度立刻动
- [x] 操作按钮加大 + hover 背景 + actingId 加载态
- [x] questionCount = 题目 × 目标数，分母修正

### Judge 评分修复
- [x] 模板 Prompt 替代 JSON Schema（mimo 兼容）
- [x] extractJson 提前到 BeanOutputConverter 之前
- [x] base-url 修正，去掉 /v1/chat/completions 后缀

### 逻辑删除
- [x] Agent / LLM / Question 全部加 @TableLogic
- [x] QuestionServiceImpl 回退级联删除
- [x] init.sql 三表加 deleted 列

### 其他修复
- [x] Agent 表单 model 改为"可选，仅标注"
- [x] 删除弹窗加 loading 动画
- [x] Question 删除 FK 约束 → 逻辑删除解决
- [x] Agent 删除 → 逻辑删除解决
- [x] Spring AI 1.1.6 → 回退 1.1.2（yml 兼容问题）
- [x] application.yml 删除 ai 段占位符
- [x] LLM API params 修复 `{ params }` 包装

## 下一步待做功能
- [ ] Phase 3.2: 评测报告页真实数据
- [ ] Phase 4: Dashboard/Dataset/Report/System 接入真实 API
- [ ] 评测暂停/恢复
- [ ] RestTemplate 超时配置

## 阻塞 / 风险
- 无
