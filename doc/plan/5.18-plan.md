# 5.18 开发计划

## 当前进度概览
修复多轮评测核心 bug + 前端大量 UI 优化 + 新增休息页面。今天改动较大，前后端同步更新。

## 已完成事项
- [x] 时间格式化统一（所有列表/详情页 formatDate）
- [x] 表格列字号一致性（去掉各自 text-xs/text-sm）
- [x] 死代码清理（DatasetPage / SystemPage / ComparisonPage）
- [x] 评测列表页 autoRefresh 改用 refreshRunningProgress() 轻量接口
- [x] Store startTask 移除 fetchTasks()
- [x] 导航调整：仪表盘居中 + 新增休息页
- [x] ResultItem 类型新增 turnOrder
- [x] ChatPage 创建后删除（用户不需要）
- [x] 对比分析页面从 hidden 放出后又回隐藏（用户不需要）

## 新增页面

### 休息一下 /relax
- 网易云音乐热歌榜播放器（右下角悬浮，专辑封面 + 进度 + 控制）
- 每日一言（hitokoto API）
- 迷你日历（右上角悬浮）
- 40 片粉色落樱动画

## 今日优化

- [x] 启动评测任务 30s 超时修复 — evaluationApi.start()/restart() 设置 timeout: 600000（10分钟）
- [x] 进度条卡住不涨 — 后端 executeAsync() 中 completedCount 改为每轮 +1 实时写库
- [x] 进度条动画 — JS requestAnimationFrame + easeOutCubic 平滑过渡进度条宽度和百分比数字
- [x] 详情页自动加载结果 — onMounted 中判断已完成/已取消状态，自动调用 fetchResults()
- [x] 创建任务弹窗全选按钮 — 题目栏 sticky header 全选/取消全选
- [x] 题库管理全选 — DataTable 新增 head-* 槽位 + 表头 checkbox "全选" 标签
- [x] 创建任务 checkbox 修复 — @click.prevent 防浏览器原生 toggle 覆盖 Vue 状态
- [x] 创建任务 Agent/LLM 改为单选 — checkbox 方框 + 单选逻辑
- [x] Agent 选择器不展示模型名、truncate 防中文竖排

## 下一步待做功能
- [ ] Phase 4 — 报告 + 仪表盘
- [ ] 音乐播放器接入真实 API 代理
- [ ] 消息推送未读数

## 阻塞 / 风险
无
